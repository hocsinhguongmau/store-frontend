import useFavoriteStore from '../store/favoriteStore'

export const handleFavoriteAuth = async (id: string, favorite: boolean) => {
  // const { favoriteItems, setFavoriteItems, removeFavoriteItems } =
  //   useFavoriteStore()
  // if (!favorite) {
  //   if (!favoriteItems.includes(id)) {
  //     setFavoriteItems(id, favoriteItems)
  //   }
  // } else {
  //   removeFavoriteItems(id, favoriteItems)
  // }
}
